# monorepo publish defaults
auto-install-peers=true
strict-peer-dependencies=false

# public scoped packages (also set per-package publishConfig.access)
access=public

# avoid accidental git checks during CI publish
git-checks=false
