# Changesets

Hello! This monorepo uses [changesets](https://github.com/changesets/changesets).

1. `pnpm changeset` — describe changes
2. `pnpm version-packages` — bump versions
3. `pnpm release` — build + publish
